#include <stdio.h>
#include <stdint.h>
#include <inttypes.h>
#include <math.h>
#include "pam.h"
#include "particle.h"

int swarm_to_pam(struct Swarm swarm, uint8_t *raster)
{
    for (int row = 0; row < swarm.height; row++) {
        for (int col = 0; col < swarm.width; col++) {
        }
    }
    return 0;
}
